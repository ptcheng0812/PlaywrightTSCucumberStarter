//TODO: Variable Manager
export const VariableManager = {
  expandVariables<T>(value: T): T {
    //Condition: always return string, unless the following: APIRequest, APIResponse
    return value;
  }
}
